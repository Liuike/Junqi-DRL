game:
  mode: junqi_8x3
  board_variant: small

training:
  num_episodes: 50
  num_iterations: 50
  eval_every: 20
  eval_episodes: 5
  save_dir: models/drqn_test
  device: auto
  seed: null
  
agent:
  type: drqn
  network_type: spatial
  hidden_size: 128
  lr: 0.001
  gamma: 0.99
  epsilon_start: 1.0
  epsilon_min: 0.1
  epsilon_decay: 0.95
  batch_size: 32
  replay_buffer_size: 10000
  use_stratified_buffer: true
  num_segments: 4
  target_update_freq: 50
  opponent_update_freq: 100

wandb:
  enabled: false
  project: junqi-drql-test
  entity: null
  run_name: test_run
  tags: []
