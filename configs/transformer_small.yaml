game:
  mode: junqi_8x3
  board_variant: small

training:
  num_iterations: 5000
  eval_every: 100
  eval_episodes: 50
  save_dir: models/transformer
  device: auto
  seed: null

agent:
  type: transformer
  d_model: 64
  nhead: 4
  num_layers: 2
  dim_feedforward: 256
  dropout: 0.1
  
  lr_start: 0.0001
  lr_end: 0.000005
  gamma: 0.99
  gae_lambda: 0.95
  clip_coef: 0.2
  vf_coef: 0.5
  ent_coef_start: 0.02
  ent_coef_end: 0.001
  
  num_steps: 512
  minibatch_size: 32
  update_epochs: 4
  max_grad_norm: 0.5

wandb:
  enabled: false
  project: junqi-transformer
  entity: null
  run_name: transformer_small
  tags: []

