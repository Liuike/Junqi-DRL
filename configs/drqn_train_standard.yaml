game:
  mode: junqi_standard
  board_variant: standard

training:
  num_episodes: 50000
  num_iterations: 50000
  eval_every: 500
  eval_episodes: 200
  save_dir: models/drqn_standard
  device: auto
  seed: null
  
agent:
  type: drqn
  network_type: spatial
  hidden_size: 256
  
  lr: 0.0005
  gamma: 0.99
  
  epsilon_start: 1.0
  epsilon_min: 0.05
  epsilon_decay: 0.9995
  
  batch_size: 64
  replay_buffer_size: 100000
  use_stratified_buffer: true
  num_segments: 4
  
  target_update_freq: 300
  opponent_update_freq: 500

wandb:
  enabled: true
  project: junqi-drql
  entity: null
  run_name: drqn_spatial_standard_board
  tags:
    - production
    - spatial
    - junqi_standard
    - standard_board
    - dqn
    - self-play
